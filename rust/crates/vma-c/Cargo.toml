[package]
name = "vma-c"
version = "3.3.0"
edition.workspace = true
license.workspace = true
description = "C ABI drop-in for AMD Vulkan Memory Allocator (vma* / Vma*)"

[dependencies]
vma-core = { path = "../vma-core" }

[lib]
name = "VulkanMemoryAllocator"
crate-type = ["cdylib", "staticlib"]
